//https://www.geeksforgeeks.org/batch/dsa-at-your-own-pace/track/DSASP-Tree/problem/maximum-width-of-tree

yt=https://www.youtube.com/watch?v=zbZEcUV0t7k&t=2s


class Solution {
    public int maxWidth(Node root) {
        if(root==null){
            return 0;
        }
      Queue<Node>queue=new LinkedList<>();
      queue.add(root);
      int maxWidth=0;
      while(!queue.isEmpty()){
          int size=queue.size();
          maxWidth=Math.max(maxWidth,size);
          for(int i=0;i<size;i++){
              Node current=queue.remove();
              if(current.left!=null){
                  queue.add(current.left);
              }
              if(current.right!=null){
                  queue.add(current.right);
              }
          }
      }
        return maxWidth;
    }
}
